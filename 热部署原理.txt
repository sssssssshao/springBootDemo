热部署原理：使用类加载器（classloader 重新读取字节码文件到 jvm 内存）
如何纯手写一个热部署功能：
    1. 监听 class 文件是否有发生改变--版本号或者修改时间
    2. 如果 class 文件发生改变的，就使用classloader 进行重新读取。

热部署可以用于在生产环境？
    1.理论上可以（但是不推荐）--不安全
    2.热部署用于本地开发，目的是为了本地测试能够提高效率


devtoole 每次保存的时候自动重启